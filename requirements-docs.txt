# Documentation site toolchain. Pinned so a CI build and a local build produce
# the same site -- an unpinned docs build that breaks on a theme release is a
# broken link on a portfolio page.
mkdocs==1.6.1
mkdocs-material==9.5.49
